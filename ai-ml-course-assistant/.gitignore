# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
ENV/
.venv

# Environment variables
.env

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Jupyter Notebooks
.ipynb_checkpoints
*.ipynb

# Data files (ignore entire data folder - large files)
# But keep .gitkeep files to preserve directory structure
data/*
!data/.gitkeep
!data/raw/
!data/raw/.gitkeep
!data/processed/
!data/processed/.gitkeep
!data/processed/images/
!data/processed/images/.gitkeep
!data/chunks_backup/
!data/chunks_backup/.gitkeep
# Note: chroma_db is not included - it's generated by the system

# Docker test data (ignore test data but keep directory structure)
docker_test_data/*
!docker_test_data/.gitkeep
!docker_test_data/README.md

# Docker test logs
docker_test_logs/

# Model files (if downloading any)
*.bin
*.safetensors
*.gguf

# Build and setup scripts
setup.bat
setup.sh

# GitHub workflows (manage separately)
.github/workflows/test.yml
.github/workflows/deploy.yml
models/

# Evaluation results
eval/results/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Distribution / packaging
build/
dist/
*.egg-info/

# Streamlit
.streamlit/

# Temporary files
*.tmp
temp/

# Docker - Keep configuration files, ignore local overrides
# docker-compose.override.yml  # Uncomment if using local overrides

# Backup and utility scripts (local only)
restore_from_backup.py