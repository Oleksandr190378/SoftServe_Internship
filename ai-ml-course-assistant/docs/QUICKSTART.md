# Quick Start Guide - Data Ingestion

## 🚀 Швидкий старт

### Крок 1: Встановити залежності

```powershell
cd c:\Users\Користувач\Documents\python_1\softserve\ai-ml-course-assistant
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

### Крок 2: Завантажити тестові papers з arXiv

**Варіант A: Кілька відомих papers (рекомендовано для тесту)**
```powershell
python ingest/download_arxiv.py --num-papers 5 --mode curated
```

Це завантажить 5 важливих papers:
1. "Attention Is All You Need" (Transformers)
2. "Deep Residual Learning" (ResNet)
3. "Adam Optimizer"
4. "Batch Normalization"
5. "Dropout"

**Варіант B: Пошук за запитом**
```powershell
python ingest/download_arxiv.py --num-papers 10 --mode search --query "neural network"
```

### Крок 3: Витягнути зображення з PDFs

```powershell
python ingest/extract_images.py
```

Це:
- Витягне всі діаграми/графіки з papers (розмір > 100px)
- Збереже metadata для кожного зображення
- Витягне текст з PDF

### Крок 4: Перевірити результат

```powershell
python ingest/test_extraction.py
```

Це покаже:
- ✅ Скільки papers завантажено
- ✅ Скільки зображень витягнуто
- ✅ Статистику (розміри, кількість сторінок, слів)
- 🖼️ Опціонально відкриє 3 приклади зображень

---

## 📊 Очікувані результати

Після виконання кроків 2-3 ви матимете:

```
data/
├── raw/
│   └── papers/
│       ├── arxiv_1706_03762.pdf          # Transformer paper
│       ├── arxiv_1512_03385.pdf          # ResNet paper
│       ├── ...
│       └── papers_metadata.json          # Metadata про papers
│
└── processed/
    ├── images/
    │   ├── arxiv_1706_03762/
    │   │   ├── arxiv_1706_03762_img_001.png   # Transformer diagram
    │   │   ├── arxiv_1706_03762_img_002.png   # Attention formula
    │   │   └── ...
    │   ├── arxiv_1512_03385/
    │   │   └── ... (ResNet architectures)
    │   └── ...
    ├── images_metadata.json              # Metadata про зображення
    └── documents_metadata.json           # Metadata про документи
```

**Приклад статистики (для 5 papers):**
- 📄 Документів: 5
- 📑 Сторінок: ~50-70
- 💬 Слів: ~30,000-50,000
- 🖼️ Зображень: ~50-100 (залежить від papers)
- 💾 Розмір зображень: ~20-50 MB

---

## 🔍 Що перевіряти:

### 1. Чи є зображення з діаграмами?
Відкрийте папку: `data/processed/images/`

Ви повинні побачити:
- ✅ Архітектурні діаграми (CNN, RNN, Transformer)
- ✅ Графіки (training curves, loss plots)
- ✅ Формули (якщо рендерені як зображення)
- ✅ Блок-схеми алгоритмів

### 2. Чи витягнувся текст?
Перевірте `documents_metadata.json`:
```json
{
  "doc_id": "arxiv_1706_03762",
  "title": "Attention Is All You Need",
  "word_count": 7234,  // <-- Повинно бути > 0
  "num_images": 8,     // <-- Повинно бути > 0
  ...
}
```

---

## ⚠️ Можливі проблеми

### Проблема: "arxiv library not installed"
**Рішення:**
```powershell
pip install arxiv
```

### Проблема: "PyMuPDF not installed"
**Рішення:**
```powershell
pip install PyMuPDF
```

### Проблема: Мало зображень витягнулось
**Причина:** Деякі papers мають мало візуального контенту.

**Рішення:** Завантажте більше papers або papers з категорії Computer Vision:
```powershell
python ingest/download_arxiv.py --num-papers 10 --categories cs.CV
```

### Проблема: Завантаження дуже повільне
**Причина:** arXiv API має rate limits.

**Рішення:** Скрипт автоматично робить паузи (2-3 сек між завантаженнями). Це нормально!

---

## 🎯 Наступні кроки

Після успішного витягування даних:

1. ✅ Перегляньте зображення вручну
2. ✅ Перевірте metadata файли
3. 🔜 Переходьте до **Phase 2: Indexing** (створення embeddings)

---

## 💡 Корисні команди

**Переглянути структуру даних:**
```powershell
tree /F data
```

**Порахувати кількість зображень:**
```powershell
Get-ChildItem -Recurse data\processed\images\*.png | Measure-Object
```

**Перевірити розмір даних:**
```powershell
Get-ChildItem -Recurse data | Measure-Object -Property Length -Sum
```

---

## 📚 Документація скриптів

### download_arxiv.py
```
Options:
  --num-papers N     Кількість papers (default: 10)
  --mode curated     Відомі papers (рекомендовано)
  --mode search      Пошук за запитом
  --categories       cs.LG,cs.AI,cs.CV
  --output DIR       Куди зберігати PDFs
```

### extract_images.py
```
Options:
  --input DIR        Папка з PDFs
  --output DIR       Куди зберігати зображення
  --min-size N       Мінімальний розмір зображення (px)
```

---

**Готово до тестування!** 🚀
